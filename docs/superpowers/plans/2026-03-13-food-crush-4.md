# food-crush-4 Implementation Plan

> **For agentic workers:** REQUIRED: 이 플랜은 **Agent Team** 방식으로 실행한다 (subagent-driven-development 사용 금지).
> 독립 청크는 TeamCreate로 팀원 병렬 실행. 의존성이 있는 태스크는 순차 실행.

**Goal:** food-crush-2와 동일한 Match-3 게임을 PixiJS v8 WebGL 렌더러로 재구현하여 모바일 웹뷰 성능 개선.

**Architecture:** core/state/screens/ui/audio 레이어는 food-crush-2에서 복사. render/ 레이어만 PixiJS v8으로 재작성. game-screen.ts는 3개 DOM 결합 지점만 수정. EventBus 인터페이스 변경 없음.

**Tech Stack:** TypeScript 5.x, Vite 7, PixiJS v8 (^8.0.0), gsap (^3.12.0), Tailwind CSS 4, Vitest

**Spec:** `docs/superpowers/specs/2026-03-13-food-crush-4-design.md`

---

## 핵심 이해: DOM 결합 지점 3곳

food-crush-2의 game-screen.ts에서 DOM에 직접 의존하는 부분:

1. **rainbow 애니메이션** (line 401): `getBlock()?.el` — HTMLElement 접근
   → 해결: `rainbowSuckIn(positions: Position[], ...)` 으로 시그니처 변경

2. **rocket DOM 스프라이트** (lines 483-542): `getBoardElement()`로 HTMLElement 받아 rocket div 추가
   → 해결: `getBoardElement()` 제거, PixiJS Text 스프라이트로 대체

3. **EffectsCanvas 임포트** (line 11): `EffectsCanvas` → `EffectsLayer` 로 교체

나머지 모든 코드는 변경 없이 복사.

## 구현 시 주의사항 (리뷰 지적 사항)

1. **`board-renderer.ts` 입력 변수**: `setupInput()` 내 클로저 변수 4개(`startPos`, `startPixel`, `swapFired`, `pendingSwipeTo`)를 **class field**로 선언하고 `resetInput()`에서 초기화해야 함 — 빈 메서드로 두면 입력 잠김 버그 발생

2. **`block-sprite.ts` 시각 표현**: 이모지(`Text`) + 단색 `Graphics` 배경 방식 사용 (PNG 에셋 로딩 없음). 스펙의 "PNG 재사용"은 초기 구현에서 이모지 폴백으로 대체. 테스트 사이클에서 문제 시 PNG Sprite로 전환.

3. **`rainbowSuckIn` 색상**: `Position[]`에 블록 타입 정보 없음 → 무지개 팔레트 인덱스로 색상 선택: `const RAINBOW_PALETTE = ['#ff0080', '#ffd700', '#00e676', '#00b0ff', '#f97316']`

4. **`game-screen.ts` async 초기화**: constructor에서 `mount()` fire-and-forget 사용 시 PixiJS 초기화 전 사용자 입력 허용 가능. **`showLevelIntro()`를 `mount().then()` 내부로 이동**하여 순서 보장.

5. **`setSelected()` 시각 효과**: selected=true 시 `bg.tint = 0xffdd00` (황금색)으로 선택 상태 표시.

---

## File Map

### 새로 생성
```
food-crush-4/
├── package.json                          # pixi.js + gsap 추가
├── vite.config.ts                        # food-crush-2와 동일
├── tsconfig.json                         # food-crush-2와 동일
├── index.html                            # food-crush-2와 동일
├── src/
│   ├── main.ts                           # food-crush-2와 동일
│   ├── render/
│   │   ├── pixi-app.ts                   # NEW: PixiJS Application 싱글톤
│   │   ├── block-sprite.ts               # NEW: BlockView 대체 (PixiJS Container)
│   │   ├── board-renderer.ts             # NEW: BoardRenderer PixiJS 구현
│   │   └── effects-layer.ts              # NEW: EffectsCanvas 대체 (Position[] API)
│   ├── screens/
│   │   └── game-screen.ts                # MODIFY: DOM 결합 지점 3곳 수정
│   ├── core/                             # COPY: food-crush-2 그대로
│   ├── state/                            # COPY: food-crush-2 그대로
│   ├── screens/ (game-screen.ts 제외)    # COPY: food-crush-2 그대로
│   ├── ui/                               # COPY: food-crush-2 그대로
│   └── audio/                            # COPY: food-crush-2 그대로
├── public/assets/                        # COPY: food-crush-2 그대로
└── tests/                                # COPY: food-crush-2 그대로
```

---

## Chunk 1: 프로젝트 초기화

### Task 1: 파일 복사 및 프로젝트 구조 생성

**Files:**
- Create: `food-crush-4/` (프로젝트 루트)

- [ ] **Step 1: 디렉토리 생성 및 food-crush-2 파일 복사**

```bash
cd /Users/jaejin/projects/toy/game-arcade
cp -r food-crush-2 food-crush-4
cd food-crush-4
# render/ 디렉토리 비우기 (새로 작성할 파일들)
rm -f src/render/board-renderer.ts src/render/block-view.ts src/render/effects-canvas.ts src/render/screen-shake.ts
mkdir -p src/render
```

- [ ] **Step 2: package.json 업데이트 — pixi.js + gsap 추가**

`food-crush-4/package.json` 수정:
```json
{
  "name": "food-crush-4",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "pixi.js": "^8.0.0",
    "gsap": "^3.12.0"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "@types/node": "^22.0.0",
    "vite": "^7.3.1",
    "vitest": "^3.0.0",
    "tailwindcss": "^4.1.0",
    "@tailwindcss/vite": "^4.1.0"
  }
}
```

- [ ] **Step 3: 의존성 설치**

```bash
cd food-crush-4
npm install
```

Expected: `node_modules/pixi.js`, `node_modules/gsap` 설치됨.

- [ ] **Step 4: 기존 테스트 통과 확인 (core/ 로직 테스트)**

```bash
npm run test
```

Expected: 8개 테스트 파일 모두 PASS. core/ 테스트는 DOM 의존 없음.

- [ ] **Step 5: 커밋**

```bash
cd /Users/jaejin/projects/toy/game-arcade
git add food-crush-4/
git commit -m "feat(food-crush-4): 프로젝트 초기화 — pixi.js v8 + gsap 추가"
```

---

## Chunk 2: PixiJS render/ 레이어 구현

> **병렬 실행 가능**: Task 2 (pixi-app.ts), Task 3 (block-sprite.ts) 는 독립적이므로 동시 작성 가능.
> Task 4 (board-renderer.ts) 는 Task 2, 3 완료 후 작성.
> Task 5 (effects-layer.ts) 는 Task 2 와 독립.

### Task 2: pixi-app.ts — PixiJS Application 싱글톤

**Files:**
- Create: `food-crush-4/src/render/pixi-app.ts`

- [ ] **Step 1: pixi-app.ts 작성**

```typescript
// src/render/pixi-app.ts
import { Application } from 'pixi.js'

let app: Application | null = null

export async function initPixiApp(container: HTMLElement): Promise<Application> {
  app = new Application()
  await app.init({
    resizeTo: container,
    backgroundAlpha: 0,
    antialias: true,
    resolution: window.devicePixelRatio || 1,
    autoDensity: true,
  })
  container.appendChild(app.canvas)
  app.canvas.style.position = 'absolute'
  app.canvas.style.inset = '0'
  app.canvas.style.pointerEvents = 'none' // 터치 이벤트는 PixiJS Container가 처리
  return app
}

export function getPixiApp(): Application {
  if (!app) throw new Error('PixiJS App not initialized')
  return app
}

export function destroyPixiApp(): void {
  app?.destroy(true, { children: true })
  app = null
}
```

- [ ] **Step 2: 커밋**

```bash
git add food-crush-4/src/render/pixi-app.ts
git commit -m "feat(food-crush-4): pixi-app.ts — PixiJS Application 싱글톤"
```

---

### Task 3: block-sprite.ts — BlockView 대체

**Files:**
- Create: `food-crush-4/src/render/block-sprite.ts`

배경: food-crush-2의 `BlockView`(div 기반)를 `BlockSprite`(PixiJS Container 기반)으로 교체.
동일한 퍼블릭 API 유지 (단, `el: HTMLElement` 제거 — game-screen.ts 수정으로 불필요).

- [ ] **Step 1: block-sprite.ts 작성**

```typescript
// src/render/block-sprite.ts
import { Container, Graphics, Text } from 'pixi.js'
import { gsap } from 'gsap'
import {
  CELL_SIZE, BLOCK_COLORS, BLOCK_EMOJIS,
  type BlockType, type Position
} from '@/core/types'

const BLOCK_BG_COLORS: Record<number, number> = {
  0: 0xfb923c, // CHICKEN
  1: 0xf87171, // COLA
  2: 0xfbbf24, // FRIES
  3: 0xb45309, // BURGER
  4: 0x4ade80, // PIZZA
}

export class BlockSprite extends Container {
  private bg: Graphics
  private label: Text
  private hintTween: gsap.core.Tween | null = null

  constructor(public blockType: BlockType, public pos: Position) {
    super()
    const size = CELL_SIZE - 6

    this.bg = new Graphics()
    this.bg.roundRect(0, 0, size, size, 10)
    this.bg.fill({ color: BLOCK_BG_COLORS[blockType], alpha: 1 })
    this.addChild(this.bg)

    this.label = new Text({
      text: BLOCK_EMOJIS[blockType],
      style: { fontSize: CELL_SIZE * 0.55 }
    })
    this.label.anchor.set(0.5)
    this.label.x = size / 2
    this.label.y = size / 2
    this.addChild(this.label)

    this.updatePosition()
  }

  updatePosition(): void {
    this.x = this.pos.col * CELL_SIZE + 3
    this.y = this.pos.row * CELL_SIZE + 3
  }

  animatePop(): Promise<void> {
    return new Promise(resolve => {
      gsap.to(this.scale, {
        x: 1.3, y: 1.3, duration: 0.08,
        onComplete: () => {
          gsap.to(this.scale, {
            x: 0, y: 0, duration: 0.1,
            onComplete: () => {
              this.destroy({ children: true })
              resolve()
            }
          })
        }
      })
    })
  }

  animateDrop(fromRow: number): Promise<void> {
    const targetY = this.pos.row * CELL_SIZE + 3
    const startY = fromRow * CELL_SIZE + 3
    this.y = startY
    return new Promise(resolve => {
      gsap.to(this, {
        y: targetY, duration: 0.18, ease: 'bounce.out',
        onComplete: resolve
      })
    })
  }

  spawnAnimate(delayMs = 0): void {
    this.scale.set(0)
    gsap.to(this.scale, {
      x: 1, y: 1, duration: 0.2, ease: 'back.out(1.7)',
      delay: delayMs / 1000
    })
  }

  startHint(): void {
    this.hintTween = gsap.to(this.scale, {
      x: 1.15, y: 1.15, duration: 0.4,
      yoyo: true, repeat: -1, ease: 'sine.inOut'
    })
  }

  stopHint(): void {
    this.hintTween?.kill()
    this.hintTween = null
    gsap.to(this.scale, { x: 1, y: 1, duration: 0.1 })
  }

  setSelected(selected: boolean): void {
    if (selected) {
      this.bg.tint = 0xffdd00  // 황금색으로 선택 상태 표시
      gsap.to(this.scale, { x: 1.1, y: 1.1, duration: 0.1 })
    } else {
      this.bg.tint = 0xffffff  // 원래 색상으로 복귀
      gsap.to(this.scale, { x: 1, y: 1, duration: 0.1 })
      this.updatePosition()
    }
  }

  destroy(options?: Parameters<Container['destroy']>[0]): void {
    this.hintTween?.kill()
    super.destroy(options)
  }
}
```

- [ ] **Step 2: 커밋**

```bash
git add food-crush-4/src/render/block-sprite.ts
git commit -m "feat(food-crush-4): block-sprite.ts — PixiJS Container 기반 블록"
```

---

### Task 4: board-renderer.ts — PixiJS 보드 렌더러

**Files:**
- Create: `food-crush-4/src/render/board-renderer.ts`

배경: food-crush-2의 board-renderer.ts (DOM 기반)를 PixiJS Container 기반으로 교체.
동일한 퍼블릭 인터페이스 유지. `getBoardElement()` → `getBoardContainer()` 로 변경.

- [ ] **Step 1: board-renderer.ts 작성**

```typescript
// src/render/board-renderer.ts
import { Container, Graphics, Text } from 'pixi.js'
import { gsap } from 'gsap'
import { initPixiApp } from './pixi-app'
import { BlockSprite } from './block-sprite'
import {
  BOARD_COLS, BOARD_ROWS, CELL_SIZE,
  type BoardGrid, type BlockType, type Position, EMPTY
} from '@/core/types'
import { eventBus } from '@/state/event-bus'
import { soundManager } from '@/audio/sound-manager'

export class BoardRenderer {
  private pixiContainer: Container
  private blocks: Map<string, BlockSprite> = new Map()
  private selectedPos: Position | null = null
  private inputLocked = false
  private toolMode = false
  private boardWidth = BOARD_COLS * CELL_SIZE
  private boardHeight = BOARD_ROWS * CELL_SIZE

  constructor(private domContainer: HTMLElement) {
    this.pixiContainer = new Container()
    this.setupBackground()
    this.setupInput()
  }

  async mount(): Promise<void> {
    const app = await initPixiApp(this.domContainer)
    app.stage.addChild(this.pixiContainer)
    // 보드를 컨테이너 중앙에 배치
    this.updateBoardPosition()
    window.addEventListener('resize', () => this.updateBoardPosition())
  }

  private updateBoardPosition(): void {
    const w = this.domContainer.clientWidth
    const h = this.domContainer.clientHeight
    this.pixiContainer.x = (w - this.boardWidth) / 2
    this.pixiContainer.y = (h - this.boardHeight) / 2
  }

  private setupBackground(): void {
    const bg = new Graphics()
    bg.roundRect(0, 0, this.boardWidth, this.boardHeight, 16)
    bg.fill({ color: 0x000000, alpha: 0.55 })
    this.pixiContainer.addChild(bg)
  }

  private key(col: number, row: number): string { return `${col},${row}` }

  renderBoard(board: BoardGrid, animated = false): void {
    // 기존 블록 제거
    this.blocks.forEach(b => { if (!b.destroyed) b.destroy({ children: true }) })
    this.blocks.clear()

    for (let col = 0; col < BOARD_COLS; col++) {
      for (let row = 0; row < BOARD_ROWS; row++) {
        if (board[col][row] === EMPTY) continue
        const sprite = new BlockSprite(board[col][row] as BlockType, { col, row })
        this.blocks.set(this.key(col, row), sprite)
        this.pixiContainer.addChild(sprite)
        if (animated) {
          sprite.spawnAnimate(row * 18 + col * 8)
        }
      }
    }
  }

  setToolMode(active: boolean): void {
    this.toolMode = active
    if (!active && this.selectedPos) {
      this.blocks.get(this.key(this.selectedPos.col, this.selectedPos.row))?.setSelected(false)
      this.selectedPos = null
    }
  }

  lockInput(): void { this.inputLocked = true }
  unlockInput(): void {
    if (this.selectedPos) {
      this.blocks.get(this.key(this.selectedPos.col, this.selectedPos.row))?.setSelected(false)
      this.selectedPos = null
    }
    this.inputLocked = false
  }

  private setupInput(): void {
    // PixiJS pointer 이벤트를 위해 domContainer에 직접 이벤트 리스너 등록
    // (canvas.style.pointerEvents = 'none' 이므로 DOM에서 처리)
    const el = this.domContainer
    let startPos: Position | null = null
    let startPixel: { x: number; y: number } | null = null
    let swapFired = false
    let pendingSwipeTo: Position | null = null
    const SWIPE_THRESHOLD = CELL_SIZE / 3

    el.addEventListener('pointerdown', (e) => {
      if (this.inputLocked) return
      startPos = this.posFromEvent(e)
      startPixel = { x: e.clientX, y: e.clientY }
      swapFired = false
      pendingSwipeTo = null
      el.setPointerCapture(e.pointerId)
    })

    el.addEventListener('pointermove', (e) => {
      if (this.inputLocked || !startPos || !startPixel || swapFired) return
      const dx = e.clientX - startPixel.x
      const dy = e.clientY - startPixel.y
      let direction: Position | null = null
      if (Math.abs(dx) > Math.abs(dy) && Math.abs(dx) > SWIPE_THRESHOLD) {
        direction = { col: startPos.col + Math.sign(dx), row: startPos.row }
      } else if (Math.abs(dy) > Math.abs(dx) && Math.abs(dy) > SWIPE_THRESHOLD) {
        direction = { col: startPos.col, row: startPos.row + Math.sign(dy) }
      }
      if (direction && direction.col >= 0 && direction.col < BOARD_COLS &&
          direction.row >= 0 && direction.row < BOARD_ROWS) {
        swapFired = true
        pendingSwipeTo = direction
      }
    })

    el.addEventListener('pointerup', (e) => {
      if (this.inputLocked) { this.resetInput(); return }
      if (!startPos) { this.resetInput(); return }

      if (swapFired && pendingSwipeTo) {
        if (this.toolMode) {
          eventBus.emit('board:cell-tapped', { col: startPos.col, row: startPos.row })
        } else {
          eventBus.emit('board:swap-requested', { from: startPos, to: pendingSwipeTo })
        }
        this.resetInput()
        return
      }

      const endPos = this.posFromEvent(e)
      if (endPos && endPos.col === startPos.col && endPos.row === startPos.row) {
        eventBus.emit('board:cell-tapped', { col: endPos.col, row: endPos.row })
        if (!this.toolMode) {
          if (this.selectedPos) {
            this.blocks.get(this.key(this.selectedPos.col, this.selectedPos.row))?.setSelected(false)
            const sdx = endPos.col - this.selectedPos.col
            const sdy = endPos.row - this.selectedPos.row
            if (Math.abs(sdx) + Math.abs(sdy) === 1) {
              eventBus.emit('board:swap-requested', { from: this.selectedPos, to: endPos })
            }
            this.selectedPos = null
          } else {
            this.selectedPos = endPos
            this.blocks.get(this.key(endPos.col, endPos.row))?.setSelected(true)
          }
        }
      }
      this.resetInput()
    })

    el.addEventListener('pointercancel', () => this.resetInput())
  }

  private resetInput(): void {
    // startPos, startPixel 등은 setupInput 클로저 내부이므로 직접 리셋 불가
    // → setupInput 리팩토링: 필드로 이동 (실제 구현 시 조정)
  }

  private posFromEvent(e: PointerEvent): Position | null {
    const rect = this.domContainer.getBoundingClientRect()
    const offsetX = (rect.width - this.boardWidth) / 2
    const offsetY = (rect.height - this.boardHeight) / 2
    const col = Math.floor((e.clientX - rect.left - offsetX) / CELL_SIZE)
    const row = Math.floor((e.clientY - rect.top - offsetY) / CELL_SIZE)
    if (col < 0 || col >= BOARD_COLS || row < 0 || row >= BOARD_ROWS) return null
    return { col, row }
  }

  async animateSwap(from: Position, to: Position): Promise<void> {
    const a = this.blocks.get(this.key(from.col, from.row))
    const b = this.blocks.get(this.key(to.col, to.row))
    if (!a || !b) return

    soundManager.play('swap')
    this.blocks.set(this.key(from.col, from.row), b)
    this.blocks.set(this.key(to.col, to.row), a)
    a.pos = { col: to.col, row: to.row }
    b.pos = { col: from.col, row: from.row }

    await Promise.all([
      new Promise<void>(r => gsap.to(a, { x: a.pos.col * CELL_SIZE + 3, y: a.pos.row * CELL_SIZE + 3, duration: 0.16, onComplete: r })),
      new Promise<void>(r => gsap.to(b, { x: b.pos.col * CELL_SIZE + 3, y: b.pos.row * CELL_SIZE + 3, duration: 0.16, onComplete: r })),
    ])
  }

  startHint(from: Position, to: Position): void {
    this.blocks.get(this.key(from.col, from.row))?.startHint()
    this.blocks.get(this.key(to.col, to.row))?.startHint()
  }

  stopHint(): void { this.blocks.forEach(b => b.stopHint()) }

  getBlock(col: number, row: number): BlockSprite | undefined {
    return this.blocks.get(this.key(col, row))
  }

  removeBlock(col: number, row: number): void {
    this.blocks.delete(this.key(col, row))
  }

  moveBlock(from: Position, to: Position): void {
    const sprite = this.blocks.get(this.key(from.col, from.row))
    if (!sprite) return
    this.blocks.delete(this.key(from.col, from.row))
    this.blocks.set(this.key(to.col, to.row), sprite)
  }

  addBlock(pos: Position, blockType: BlockType): BlockSprite {
    const sprite = new BlockSprite(blockType, pos)
    this.blocks.set(this.key(pos.col, pos.row), sprite)
    this.pixiContainer.addChild(sprite)
    return sprite
  }

  /** rocket 애니메이션용: PixiJS Container 반환 */
  getBoardContainer(): Container { return this.pixiContainer }

  /** 보드의 화면상 좌측 상단 좌표 반환 (effects 좌표 계산용) */
  getBoardOffset(): { x: number; y: number } {
    const rect = this.domContainer.getBoundingClientRect()
    return {
      x: rect.left + (rect.width - this.boardWidth) / 2,
      y: rect.top + (rect.height - this.boardHeight) / 2,
    }
  }

  destroy(): void {
    this.blocks.forEach(b => b.destroy({ children: true }))
    this.pixiContainer.destroy({ children: true })
  }
}
```

**Note:** `setupInput` 의 `resetInput` 패턴은 실제 구현 시 클로저 변수를 class field로 이동하여 정리 필요.

- [ ] **Step 2: 커밋**

```bash
git add food-crush-4/src/render/board-renderer.ts
git commit -m "feat(food-crush-4): board-renderer.ts — PixiJS Container 기반 보드"
```

---

### Task 5: effects-layer.ts — EffectsCanvas 교체

**Files:**
- Create: `food-crush-4/src/render/effects-layer.ts`

배경: `EffectsCanvas`를 복사하되 `rainbowSuckIn` 시그니처 변경.
- 변경 전: `rainbowSuckIn(targetElements: HTMLElement[], centerX, centerY)`
- 변경 후: `rainbowSuckIn(positions: Position[], centerX, centerY)`

Canvas 2D 파티클은 그대로 유지 (이미 충분히 빠름). 주요 성능 개선은 블록 렌더링(PixiJS)에서 이루어짐.

- [ ] **Step 1: effects-layer.ts 작성 (effects-canvas.ts 복사 + 수정)**

`food-crush-2/src/render/effects-canvas.ts`를 복사해서 다음만 변경:

1. 파일명: `effects-canvas.ts` → `effects-layer.ts`
2. 클래스명: `EffectsCanvas` → `EffectsLayer`
3. `rainbowSuckIn` 메서드 교체:

```typescript
// effects-canvas.ts의 rainbowSuckIn 대체
rainbowSuckIn(positions: Position[], centerX: number, centerY: number): Promise<void> {
  const RAINBOW_PALETTE = ['#ff0080', '#ffd700', '#00e676', '#00b0ff', '#f97316']
  return new Promise(resolve => {
    const DURATION = 400
    positions.forEach((pos, i) => {
      const elCX = this.boardOffsetX + pos.col * CELL_SIZE + CELL_SIZE / 2
      const elCY = this.boardOffsetY + pos.row * CELL_SIZE + CELL_SIZE / 2
      const dX = centerX - elCX
      const dY = centerY - elCY
      const color = RAINBOW_PALETTE[i % RAINBOW_PALETTE.length]

      setTimeout(() => {
        for (let j = 0; j < 4; j++) {
          const angle = Math.atan2(dY, dX) + (Math.random() - 0.5) * 0.5
          const speed = Math.sqrt(dX * dX + dY * dY) / (DURATION / 16)
          this.particles.push({
            x: elCX, y: elCY,
            vx: Math.cos(angle) * speed,
            vy: Math.sin(angle) * speed,
            life: 1, maxLife: 1, size: 3 + Math.random() * 2, color,
          })
        }
      }, i * 15)
    })
    setTimeout(resolve, DURATION + positions.length * 15)
  })
}
```

- [ ] **Step 2: 커밋**

```bash
git add food-crush-4/src/render/effects-layer.ts
git commit -m "feat(food-crush-4): effects-layer.ts — rainbowSuckIn Position[] 기반으로 변경"
```

---

## Chunk 3: game-screen.ts 통합

### Task 6: game-screen.ts — DOM 결합 지점 3곳 수정

**Files:**
- Modify: `food-crush-4/src/screens/game-screen.ts`

3개의 변경 지점만 수정. 나머지 코드 변경 없음.

- [ ] **Step 1: import 변경**

```typescript
// 변경 전
import { BoardRenderer } from '@/render/board-renderer'
import { EffectsCanvas } from '@/render/effects-canvas'
import { ScreenShake } from '@/render/screen-shake'

// 변경 후
import { BoardRenderer } from '@/render/board-renderer'
import { EffectsLayer } from '@/render/effects-layer'
// ScreenShake 제거 (CSS transform shake를 boardArea DOM에 직접 구현)
```

- [ ] **Step 2: 생성자 내 초기화 수정**

constructor에서 `EffectsCanvas` → `EffectsLayer` 교체. `mount()` async 문제 해결:
**`showLevelIntro()`와 `renderBoard()`를 모두 `mount().then()` 내부로 이동** — constructor에서 `await` 사용 불가하므로 `.then()` 체이닝으로 처리.

```typescript
// 변경 전 (game-screen.ts constructor, 맨 끝 부분)
this.boardRenderer = new BoardRenderer(boardArea)
this.boardRenderer.renderBoard(this.boardLogic.getBoard())
this.effects = new EffectsCanvas(boardArea)
this.shake = new ScreenShake(boardArea)
// ...
this.showLevelIntro(config.level, gameState.getGoals())

// 변경 후
this.boardRenderer = new BoardRenderer(boardArea)
this.effects = new EffectsLayer(boardArea)
this.shake = {
  shake: (intensity: number, duration = 200) => {
    boardArea.style.transition = `transform ${duration}ms ease-out`
    boardArea.style.transform = `translate(${intensity}px, 0)`
    setTimeout(() => { boardArea.style.transform = '' }, duration / 2)
  }
} as ScreenShake

// mount 완료 후 renderBoard + showLevelIntro 순서 보장
this.boardRenderer.mount().then(() => {
  this.boardRenderer.renderBoard(this.boardLogic.getBoard())
  this.showLevelIntro(config.level, gameState.getGoals())
})
```

- [ ] **Step 3: rainbow 애니메이션 수정**

```typescript
// 변경 전 (handleCellTap, lines 399-408)
const targetEls = targets
  .map((p: Position) => this.boardRenderer.getBlock(p.col, p.row)?.el)
  .filter((el: HTMLElement | undefined): el is HTMLElement => el !== undefined)
if (targetEls.length > 0) {
  await this.effects.rainbowSuckIn(
    targetEls,
    this.effects.boardCenterX,
    this.effects.boardCenterY
  )

// 변경 후
if (targets.length > 0) {
  await this.effects.rainbowSuckIn(
    targets,
    this.effects.boardCenterX,
    this.effects.boardCenterY
  )
```

- [ ] **Step 4: rocket DOM 스프라이트 → PixiJS Text 스프라이트**

```typescript
// animateRocketLaunch 내부
// 변경 전: DOM 접근
const boardEl = this.boardRenderer.getBoardElement()
// ...
const sprite = document.createElement('div')
sprite.className = 'rocket-sprite'
sprite.textContent = '🚀'
boardEl.appendChild(sprite)

// 변경 후: PixiJS Text 사용
import { Text as PixiText } from 'pixi.js'
const boardContainer = this.boardRenderer.getBoardContainer()
// ...
const rocketSprite = new PixiText({ text: '🚀', style: { fontSize: 28 } })
rocketSprite.anchor.set(0.5)
rocketSprite.x = startX
rocketSprite.y = startY
boardContainer.addChild(rocketSprite)
// 이동 애니메이션은 gsap으로
// ...
// 완료 후: rocketSprite.destroy()
```

- [ ] **Step 5: 빌드 확인**

```bash
npm run build
```

Expected: TypeScript 타입 에러 없이 빌드 성공.

- [ ] **Step 6: 커밋**

```bash
git add food-crush-4/src/screens/game-screen.ts
git commit -m "feat(food-crush-4): game-screen.ts — DOM 결합 지점 3곳 PixiJS 기반으로 교체"
```

---

### Task 7: 개발 서버 실행 및 초기 확인

**Files:**
- (없음 — 실행 확인)

- [ ] **Step 1: 개발 서버 시작**

```bash
npm run dev
# → localhost:5173 (또는 vite 기본 포트)
```

- [ ] **Step 2: 브라우저에서 기본 화면 확인**

Cover screen이 정상 표시되는지 확인. Console 에러 없는지 확인.

- [ ] **Step 3: 최종 빌드 확인**

```bash
npm run build && npm run preview
```

---

## Chunk 4: Chrome 테스트 사이클 (5회)

> **순차 실행**: 각 사이클은 이전 사이클의 수정 결과를 바탕으로 진행.

### 테스트 프로토콜

각 사이클에서 확인할 항목:

| # | 항목 | 확인 방법 |
|---|------|---------|
| 1 | Cover 화면 표시 | 게임 시작 화면 렌더링 |
| 2 | Map 화면 → 레벨 선택 | 레벨 1 버튼 클릭 |
| 3 | 게임 화면 진입 | PixiJS 보드 렌더링 (7x7 블록) |
| 4 | 블록 스왑 애니메이션 | 스와이프 → 블록 이동 자연스러움 |
| 5 | 매치 + 낙하 | 3개 이상 매치 → pop + fall |
| 6 | 파티클 이펙트 | 블록 제거 시 파티클 |
| 7 | 도구 사용 (Rocket/Bomb/Rainbow) | 각 도구 1회씩 |
| 8 | 레벨 클리어 / 실패 | Clear/Fail 화면 진입 |

food-crush-2 (`localhost:3001`)와 food-crush-4 (`localhost:5173`) 동시 열어 비교.

### Cycle 1

- [ ] `npm run dev` — 서버 실행
- [ ] Claude in Chrome으로 게임 플레이 (테스트 프로토콜 8개 항목 순서대로)
- [ ] 발견된 버그/차이점 목록 작성
- [ ] 수정 구현
- [ ] `git commit -m "fix(food-crush-4): Cycle 1 — [수정 내용 요약]"`

### Cycle 2

- [ ] Cycle 1 수정 후 재빌드: `npm run build`
- [ ] Claude in Chrome으로 재테스트
- [ ] 발견된 버그/차이점 목록 작성
- [ ] 수정 구현
- [ ] `git commit -m "fix(food-crush-4): Cycle 2 — [수정 내용 요약]"`

### Cycle 3

- [ ] Cycle 2 수정 후 재테스트
- [ ] 주안점: 성능 비교 (애니메이션 프레임 드롭, 터치 반응)
- [ ] 수정 구현
- [ ] `git commit -m "fix(food-crush-4): Cycle 3 — [수정 내용 요약]"`

### Cycle 4

- [ ] Cycle 3 수정 후 재테스트
- [ ] 주안점: 엣지 케이스 (빠른 연속 스왑, 도구 연속 사용, 레벨 10 이상)
- [ ] 수정 구현
- [ ] `git commit -m "fix(food-crush-4): Cycle 4 — [수정 내용 요약]"`

### Cycle 5

- [ ] 최종 테스트 — 전체 게임 플로우 1회 완주
- [ ] food-crush-2와의 동작 차이 없음 확인
- [ ] `git commit -m "fix(food-crush-4): Cycle 5 — 최종 검증 완료"`

---

## 완료 기준

- [ ] `npm run test` — 모든 테스트 PASS
- [ ] `npm run build` — 빌드 성공
- [ ] Chrome 테스트 5 사이클 완료
- [ ] food-crush-2와 동일한 게임 동작 확인
- [ ] 모바일 웹뷰에서 블록 애니메이션 smooth (60fps)
